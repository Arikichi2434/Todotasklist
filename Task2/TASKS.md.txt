TASKS.md

# フロントエンド理解のための学習タスクリスト

## 最終的に説明できるようにする大きな流れ

```text
ユーザーが画面を操作する
↓
イベントが発生する
↓
JavaScriptの処理が動く
↓
Stateやデータが変わる
↓
DOMを通じて画面が更新される
↓
必要に応じてAPI・fetchでバックエンドと通信する
↓
取得したデータを画面に反映する
```

---

# 1. Webアプリ全体像を理解する

## 目的

フロントエンドがWebアプリ全体の中で何を担当しているかを説明できるようにする。

## タスク

* [ ] フロントエンド・バックエンド・DBの役割を調べる
* [ ] Webアプリを開いたときに画面が表示されるまでの流れを書く
* [ ] ユーザーが画面を操作してから結果が表示されるまでの流れを書く
* [ ] フロントエンドとバックエンドの違いを表にする
* [ ] DBがなぜ必要なのかを一文で説明する
* [ ] Webアプリ全体の構成図を簡単に描く

## 説明できるようにすること

* フロントエンドとは何か
* バックエンドとは何か
* DBとは何か
* それぞれがどうつながっているか
* ユーザー操作がどこで処理されるか

## 到達目標

> フロントエンドは、ユーザーが直接見る画面を作り、操作を受け取り、必要に応じてバックエンドと通信して画面を更新する部分である。

---

# 2. HTMLを理解する

## 目的

HTMLが画面の「構造」を作っていることを理解する。

## タスク

* [ ] よく使うHTMLタグを調べる

  * `div`
  * `p`
  * `h1`
  * `button`
  * `input`
  * `ul`
  * `li`
  * `form`
* [ ] 簡単な自己紹介ページをHTMLだけで作る
* [ ] 作ったページの各タグが何の役割をしているか説明する
* [ ] ブラウザの開発者ツールでHTML構造を確認する
* [ ] 画面のどの部分がどのHTMLに対応しているかをメモする
* [ ] HTMLだけでは見た目や動きに限界があることを確認する

## 説明できるようにすること

* HTMLは何を担当しているか
* タグとは何か
* 見出し・文章・ボタン・入力欄をどう表すか
* HTMLが画面の骨組みと言われる理由

## 到達目標

> HTMLは、見出し・文章・ボタン・入力欄など、画面に必要な部品と構造を定義するもの。

---

# 3. CSSを理解する

## 目的

CSSが画面の「見た目」と「配置」を担当していることを理解する。

## タスク

* [ ] CSSで文字色を変更する
* [ ] CSSで背景色を変更する
* [ ] CSSで余白を変更する
* [ ] `margin` と `padding` の違いを図で整理する
* [ ] `width` と `height` の意味を確認する
* [ ] `display: flex;` を使って横並びを作る
* [ ] `display: grid;` を使って2×2の配置を作る
* [ ] 同じHTMLに対してCSSだけを変え、見た目が変わることを確認する
* [ ] CSSがない場合とある場合の画面を比較する

## 説明できるようにすること

* CSSは何を担当しているか
* 色・大きさ・余白・配置をどう指定するか
* `margin` と `padding` の違い
* `width` と `height` の意味
* `flex` と `grid` が何に使われるか

## 到達目標

> CSSは、HTMLで作った部品に対して、色・大きさ・余白・配置などの見た目を指定するもの。

---

# 4. JavaScriptを理解する

## 目的

JavaScriptが画面に「動き」をつける役割を持つことを理解する。

## タスク

* [ ] ボタンをクリックしたら文字が変わる処理を作る
* [ ] 入力欄に文字を入れたら、その内容を画面に表示する処理を作る
* [ ] `const` と `let` の違いを調べる
* [ ] `if` 文で条件分岐を作る
* [ ] 配列を使って複数データを表示する
* [ ] `forEach` を使って一覧を作る
* [ ] 関数を作り、同じ処理をまとめる
* [ ] JavaScriptがない場合とある場合の画面の違いを確認する

## 説明できるようにすること

* JavaScriptは何を担当しているか
* 変数とは何か
* 条件分岐とは何か
* 配列とは何か
* 関数とは何か
* JavaScriptによって画面に動きがつく理由

## 到達目標

> JavaScriptは、ユーザーの操作に反応して、データを処理したり画面を変更したりするためのもの。

---

# 5. イベントを理解する

## 目的

ユーザー操作によって処理が動く仕組みを理解する。

## タスク

* [ ] `click` イベントを使う
* [ ] `input` イベントを使う
* [ ] `change` イベントを使う
* [ ] `addEventListener` の意味を調べる
* [ ] ボタンを押したときに関数が実行される流れを書く
* [ ] 入力欄に文字を入れたときに処理が動く流れを書く
* [ ] 「イベント発生 → 処理実行 → 画面変更」の流れを図にする
* [ ] どのHTML要素にどのイベントを登録しているかを表にする

## 説明できるようにすること

* イベントとは何か
* クリックイベントとは何か
* 入力イベントとは何か
* `addEventListener` は何をしているか
* ユーザー操作とJavaScript処理の関係

## 到達目標

> イベントとは、クリック・入力・選択など、ユーザー操作をきっかけに発生する合図である。

---

# 6. DOMを理解する

## 目的

JavaScriptがHTMLを操作する仕組みを理解する。

## タスク

* [ ] `document.querySelector()` を使ってHTML要素を取得する
* [ ] `textContent` を使って文字を書き換える
* [ ] `classList.add()` でCSSクラスを追加する
* [ ] `classList.remove()` でCSSクラスを削除する
* [ ] `innerHTML` を使って一覧を表示する
* [ ] JavaScriptからHTML要素を変更する流れを書く
* [ ] HTML上のidやclassがJavaScriptでどう使われているか確認する
* [ ] 「HTML要素を取得する処理」と「HTML要素を変更する処理」を分けて整理する

## 説明できるようにすること

* DOMとは何か
* `querySelector` は何をしているか
* `textContent` は何をしているか
* `classList` は何をしているか
* JavaScriptがHTMLを変更できる理由

## 到達目標

> DOMは、JavaScriptからHTMLの要素を取得したり変更したりするための仕組み。

---

# 7. Stateを理解する

## 目的

画面の状態が変わると、表示内容も変わることを理解する。

## タスク

* [ ] クイズアプリを例にStateを書き出す

  * 現在の問題番号
  * 選択した答え
  * 正解・不正解
  * 解説を表示するかどうか
* [ ] 検索アプリを例にStateを書き出す

  * 検索文字
  * 選択カテゴリ
  * 表示中のデータ
* [ ] ToDoアプリを例にStateを書き出す

  * タスク一覧
  * 完了済みかどうか
  * 入力中の文字
* [ ] ユーザー操作でStateが変わる例を3つ書く
* [ ] Stateが変わると画面が変わる例を3つ書く
* [ ] StateとHTML表示の対応表を作る

## 説明できるようにすること

* Stateとは何か
* なぜStateが必要なのか
* ユーザー操作でStateが変わるとはどういうことか
* Stateが変わると画面が変わるとはどういうことか

## 到達目標

> Stateとは、画面の現在の状態を表すデータであり、Stateが変わると表示内容も変わる。

---

# 8. データの流れを理解する

## 目的

データがどこから来て、どの処理を通って、どう画面に表示されるかを説明できるようにする。

## タスク

* [ ] 社員一覧アプリを例に、データの流れを書く
* [ ] クイズアプリを例に、データの流れを書く
* [ ] ToDoアプリを例に、データの流れを書く
* [ ] 検索条件によって表示データが変わる流れを書く
* [ ] 配列データからHTMLを作る処理を確認する
* [ ] 「元データ」「加工処理」「表示結果」を分けて整理する
* [ ] データが変わったときに画面がどう変わるかを図にする
* [ ] 1つの操作について「入力 → 処理 → 出力」の形で整理する

## 説明できるようにすること

* データはどこにあるか
* データをどう加工しているか
* 加工したデータをどう画面に表示しているか
* 検索・絞り込みで何が変わっているか
* 元データと表示データの違い

## 到達目標

> フロントエンドでは、データを受け取り、必要に応じて加工し、画面に表示する流れがある。

---

# 9. APIとfetchを理解する

## 目的

フロントエンドがバックエンドと通信する仕組みを理解する。

## タスク

* [ ] APIとは何かを調べる
* [ ] `fetch` とは何かを調べる
* [ ] `GET` と `POST` の違いを調べる
* [ ] フロントエンドからサーバーにデータを取りに行く流れを書く
* [ ] フロントエンドからサーバーにデータを送る流れを書く
* [ ] サーバーから返ってきたデータを画面に表示する流れを書く
* [ ] API通信で取得したデータを画面に表示する簡単な例を作る
* [ ] 通信に失敗した場合に何が起こるかを確認する
* [ ] 「画面操作 → fetch → API → レスポンス → 画面更新」の流れを図にする

## 説明できるようにすること

* APIとは何か
* fetchとは何か
* GETとPOSTの違い
* フロントエンドとバックエンドがどう通信するか
* サーバーから取得したデータをどう画面に反映するか

## 到達目標

> APIはフロントエンドとバックエンドのやり取り口であり、fetchはJavaScriptからAPIに通信するための命令である。

---

# 9番まで終わったときに説明できるべきこと

* [ ] HTMLは画面の構造を作るものだと説明できる
* [ ] CSSは見た目と配置を整えるものだと説明できる
* [ ] JavaScriptは操作に反応して処理を動かすものだと説明できる
* [ ] イベントはユーザー操作をきっかけに発生するものだと説明できる
* [ ] DOMはJavaScriptからHTMLを操作する仕組みだと説明できる
* [ ] Stateは画面の現在の状態を表すデータだと説明できる
* [ ] データがどこから来て、どう加工され、どう表示されるか説明できる
* [ ] APIはフロントエンドとバックエンドのやり取り口だと説明できる
* [ ] fetchはJavaScriptからAPIに通信する命令だと説明できる

---

# 最終確認用の説明文

以下の文章を、自分の作ったアプリを例にして説明できれば完了。

> フロントエンドでは、HTMLで画面構造を作り、CSSで見た目を整え、JavaScriptでユーザー操作に反応する。ユーザーがクリックや入力をするとイベントが発生し、JavaScriptの処理によってStateやデータが変わる。その結果、DOMを通じて画面が更新される。必要に応じてAPIを使い、fetchでバックエンドと通信し、取得したデータを画面に反映する。
